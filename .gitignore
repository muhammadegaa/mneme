node_modules/
dist/
.env
.env.local
*.log
.DS_Store
coverage/
.next/
out/
bench/results/*.json
!bench/results/.gitkeep

.gstack/

# vision-verify harness (dev only)
_*.mjs
_*.mts

# demo golden seed IS committed (reproducible hero); runtime state is not
.engram/*
!.engram/golden.json

# HyperFrames video build project (source + node_modules + renders; MP4 uploads to Devpost directly)
videos/
